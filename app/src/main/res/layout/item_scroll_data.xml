<?xml version="1.0" encoding="utf-8"?>
<android.support.v4.widget.NestedScrollView xmlns:android="http://schemas.android.com/apk/res/android"
        android:background="#F3F3F3"
        android:layout_width="match_parent"
        android:layout_height="100dp">

    <EditText
            android:background="@null"
            android:layout_width="match_parent"
            android:layout_height="10dp"
            android:text="this is inner scroll area this is inner scroll area this is inner scroll area this is inner scroll area this is inner scroll area this is inner scroll area this is inner scroll area this is inner scroll area this is inner scroll area this is inner scroll area this is inner scroll area" />
</android.support.v4.widget.NestedScrollView>
